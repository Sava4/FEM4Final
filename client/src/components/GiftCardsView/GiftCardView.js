import React from "react"
import { Layout } from "../common/Layout/index";
import {GiftCard} from "../StaticPage/GiftCard/GiftCard";

export const GiftCardView = () => {
  return (
    <Layout>
      <GiftCard/>
    </Layout>
  );
};